[tool.black]
line-length = 100
target-version = ['py312']  
include = '\.pyi?$'
exclude = '''
/(
    \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | \.env
  | env
  | venv
  | _build
  | buck-out
  | build
  | dist
  | migrations
  | static/CACHE
  | node_modules
)/
'''

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "steamscape.settings"
addopts = "--reuse-db --cov=. --cov-report=html --cov-report=term-missing:skip-covered --no-cov-on-fail -v"
python_files = ["test_*.py", "*_tests.py"]
python_functions = ["test_*"]